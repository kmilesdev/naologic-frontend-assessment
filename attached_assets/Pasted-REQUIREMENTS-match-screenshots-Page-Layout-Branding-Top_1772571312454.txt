REQUIREMENTS (match screenshots):

Page Layout + Branding

Top-left brand text “NAOLOGIC” as shown.

Page title: “Work Orders” with same sizing and spacing.

Under title: “Timescale” control with a dropdown set to “Month” by default.

Overall canvas background and whitespace must match the screenshots.

Timescale Dropdown

Dropdown options exactly as shown: Hour, Day, Week, Month.

Month is selected by default.

Dropdown styling must match screenshot (border, shadow, spacing, typography).

Timeline Grid + Work Center List

Left column: “Work Center” header and the list of work centers:

Genesis Hardware

Rodriques Electrics

Konsulting Inc

McMarrow Distribution

Spartan Manufacturing

Left column stays fixed while the timeline scrolls horizontally.

Top timeline header shows months and vertical grid lines as in screenshots.

Current Month Indicator + Tooltip

A “Current month” pill/label is shown in the header area where appropriate.

Hover tooltip “Click to add dates” appears in the correct position and styling (see screenshot).

Work Order Bars (pill blocks)

Bars render inside rows on the timeline with:

Work order name on the bar

Status pill badge inside the bar (Open / In progress / Complete / Blocked)

Bars match the colors, rounded corners, padding, and light background styles shown.

Status color styles must match:

Open (blue-ish)

In progress (purple/blue-ish)

Complete (green)

Blocked (yellow/orange)

Bars align precisely to month grid positions like screenshots (don’t worry about real dates at first—get pixel layout correct).

Hover Menu (3-dot button)

On hover over a bar (e.g., Rodriques Electrics), show the 3-dot menu button at the right edge of the bar as shown.

Clicking the 3-dot opens a small menu with:

Edit

Delete

Menu styling must match screenshot: white background, subtle shadow, spacing.

Create/Edit Drawer Panel (right side)

Clicking empty timeline space opens a right-side drawer titled:
“Work Order Details”
subtitle: “Specify the dates, name and status for this order”

Top-right buttons in the drawer: “Cancel” and “Create” (Create is primary).

Drawer fields in exact order:

Work Order Name (text input)

Status (dropdown select)

End date (text/date field)

Start date (text/date field)

Placeholder/default state must match screenshots:

Example placeholders like “Acme Inc.” when shown

Default status value “Open” when shown

Active input focus state must match screenshot (highlighted border).

Status dropdown opens with options:

Open

In progress

Complete

Blocked
and must match the dropdown styling.

Data + Behavior (minimal but correct)

Hardcode a small sample dataset to visually match the bars in the screenshots.

Overlap validation: Do not allow creating a work order that overlaps another within the same work center. If overlap happens, show an inline error message styled cleanly.

Edit opens the same drawer populated.

Delete removes the item.